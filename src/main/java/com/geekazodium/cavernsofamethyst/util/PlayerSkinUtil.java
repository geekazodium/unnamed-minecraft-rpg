package com.geekazodium.cavernsofamethyst.util;

import com.destroystokyo.paper.profile.PlayerProfile;
import com.destroystokyo.paper.profile.ProfileProperty;
import org.bukkit.Bukkit;
import org.bukkit.entity.Player;
import org.bukkit.profile.PlayerTextures;
import org.jetbrains.annotations.NotNull;

import java.util.UUID;

public class PlayerSkinUtil {
    /*public static void setPlayerSkin(Player player, UUID uuid){
        @NotNull PlayerProfile profile = player.getPlayerProfile();
        Bukkit.getServer().getOnlinePlayers().forEach().getProperties();
    }*/
}
